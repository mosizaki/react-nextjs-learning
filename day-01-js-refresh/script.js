const name = "mosi"
let age = 25

const name2 = "michael"
let age = 20

function message (userName) {
    return `hello my name is ${userName}`
}

const greetOthers = (userName) => {
    return `hello this is an arrow function and i'm ${userName}`
}

const user = {
    name: 'carl',
    age: 22,
    massege(userName) {
        console.log(`hello my name is ${userName}`)
    }
}

const skills = ['html', 'javascript', 'css']

const user = {
    name: 'mosi',
    age: 30,
    role: 'frontend developer',
    skills: ['html', 'javascript', 'css']
}